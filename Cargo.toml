[package]
name = "robutt"
version = "0.1.0"
authors = ["Erik Hollensbe <github@hollensbe.org>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
irc = { version = "^0.13", features = [ "yaml", "json", "toml" ] }
futures = ">=0"
tokio = "^0.1"
rand = ">=0"
surf = '^1'
